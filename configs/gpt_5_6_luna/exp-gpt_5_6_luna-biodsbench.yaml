Agent:
  llm:
    model_type: "Foundry"
    model_name: "gpt-5.6-luna"
    max_total_tokens: 32768
    max_input_tokens: 8192
    max_new_tokens: 32768
    log_probs: False
    # The GPT-5 generation takes only its default temperature and calls the
    # output ceiling `max_completion_tokens`; see BaseModelArgs.
    temperature: null
    token_param: "max_completion_tokens"
  n_retry: 3
  retry_delay: 10
Data:
  metadata_path: "data/metadata.json"
  data_path: "data/biodsbench"
Debugger:
  model_type: "Foundry"
  model_name: "gpt-5.6-luna"
  max_total_tokens: 32768
  max_input_tokens: 8192
  max_new_tokens: 8192
  log_probs: False
  # The GPT-5 generation takes only its default temperature and calls the
  # output ceiling `max_completion_tokens`; see BaseModelArgs.
  temperature: null
  token_param: "max_completion_tokens"
Env:
  n_retry: 3
task: "biodsbench"
credentials_path: "./credentials.toml"
work_dir: "./workdir/gpt_5_6_luna"
result_dir_tag: "gpt-5_6-luna-biodsbench"
start_idx: 0
end_idx: -1
num_steps: 15
